---
import type { HTMLAttributes } from "astro/types";
interface Props extends HTMLAttributes<"hr"> {}

const { class: className, ...props } = Astro.props;
---

<hr {...props} class:list={["bg-neutral-200", className]} />
