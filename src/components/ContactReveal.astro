<div
  class="contact-reveal is-revealed pointer-events-none absolute inset-x-0 bottom-0 h-(--contact-reveal-height) overflow-hidden rounded-b-md backdrop-blur-lg [&.is-revealed]:pointer-events-auto [&.is-revealed:focus-within]:h-(--contact-open-height) [&.is-revealed:hover]:h-(--contact-open-height)"
  data-contact-reveal
  aria-hidden="false"
>
  <div
    class="absolute inset-x-0 bottom-0 flex flex-col gap-[clamp(0.8rem,2vw,1.1rem)] p-[clamp(1rem,5%,1.75rem)]"
    data-contact-content
  >
    <p class="contact-intro">contact me with the rough version.</p>
    <a
      class="contact-email text-paper text-[clamp(1.1rem,1.7vw,1.55rem)] leading-none no-underline outline-none focus-visible:outline-2 focus-visible:outline-offset-[0.35rem] focus-visible:outline-paper"
      href="mailto:hello@example.com"
      data-contact-email
    >
      hello@example.com
    </a>
  </div>
</div>

<style>
  .contact-reveal {
    --contact-reveal-height: max-content;
    --contact-text-opacity: 1;
    --contact-open-height: 100%;

    background:
      linear-gradient(
        180deg,
        color-mix(in srgb, var(--color-black) 50%, transparent),
        color-mix(in srgb, var(--color-black) 92%, transparent)
      ),
      color-mix(in srgb, var(--color-black) 58%, transparent);
  }

  .contact-reveal[data-ready] {
    transition: height 320ms cubic-bezier(0.22, 1, 0.36, 1);
  }

  .contact-email {
    opacity: var(--contact-text-opacity);
    transform: translateY(0.35rem);
  }

  .contact-reveal[data-ready] .contact-email {
    transition:
      opacity 180ms ease,
      transform 220ms ease,
      color 240ms ease;
  }

  .contact-reveal.is-revealed .contact-email {
    transform: translateY(0);
    transition-delay: 120ms, 120ms, 0ms;
  }

  .contact-intro {
    max-width: none;
    color: color-mix(in srgb, var(--color-paper) 74%, transparent);
    font-size: clamp(0.9rem, 1.18vw, 1.08rem);
    line-height: 1.35;
    opacity: 0;
    transform: translateY(0.35rem);
    white-space: nowrap;
  }

  .contact-reveal[data-ready] .contact-intro {
    transition:
      opacity 300ms ease,
      transform 300ms ease;
  }

  .contact-reveal.is-revealed:hover .contact-intro,
  .contact-reveal.is-revealed:focus-within .contact-intro {
    opacity: 1;
    transform: translateY(0);
    transition-delay: 120ms;
  }
</style>
