---
interface Props {
  id: string;
  spaced?: boolean;
}

const { id, spaced = false } = Astro.props;
---

<p
  class:list={[
    "_story-line scroll-mt-28 text-inherit text-balance xl:whitespace-nowrap",
    spaced && "pt-7 lg:pt-9",
  ]}
  id={id}
>
  <slot />
</p>
